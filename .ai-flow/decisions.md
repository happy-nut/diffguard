# AI Flow Decisions

Record durable project decisions here so new sessions do not depend on chat memory.

## 2026-06-18

- Keep the project as a lightweight CLI, not a full autonomous agent platform.
- Do not require git worktrees; the default workflow is single writer with read-focused planner/reviewer sessions.
- Treat Claude Code and Codex CLI as adapters around the same prompt/state model.
- Store durable state in Markdown and JSON so humans can inspect and edit it.
